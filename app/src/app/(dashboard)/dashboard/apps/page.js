import AppBuilderClient from "./AppBuilderClient";

export const metadata = {
  title: "AI App Builder | Fast-Router",
  description: "Build, configure, test & export production-grade AI applications using heavy-end models.",
};

export default function AppBuilderPage() {
  return <AppBuilderClient />;
}
